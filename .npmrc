# Security: quarantine newly published packages for 7 days (npm 11.10.0+)
# Temporarily set to 0 — @neozen/* remark packages are within the 7-day window (AZ-970)
# npm doesn't support per-scope exemptions yet.
# TODO: restore to 7 once @neozen/remark-config clears quarantine (~2026-05-06)
min-release-age=0
